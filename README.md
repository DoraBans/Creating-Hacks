# Mod menu
```lua
local Screengui = instance.new("Screengui")
local frame = instance.new("Frame")
local Version = instance.new("TextLabel")
local blur = instance.new("Image")
local Button = instance.new("TextButton")
local Settings = instance.new("Textbutton")
local Plr.Character = Workspace.players.localplayer
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
local   = instance.new("Textbutton")
--prop
Screengui.name = "Screengui"

frame.name = "Frame"
frame.parent = Screengui
frame.size = UDim2.new("25,25 25,25")
frame.position = UDim2.new("0,0,0")
frame.tragable = true
frame.color = "123,123,123"

version.name = "Versions"
version.parent = Screengui
version.text = "Vape version v.4.5"
version.tragable = false
version.position = UDim2.new("255,255 255,255")

blur.name = "Blurs the Background"
blur.parent = Screengui
blur.blur = true

Button.name = "Test button"
Button.parent = frame
Button.size = UDim2.new("25,2 25,2")
Button.mousebutton1click:function(()
  local Espframe = instance.new("Frame")
  --prop
  Espframe.name = "Esp"
  Espframe.parent = Screengui
  Espframe.size = UDim2.new("25,25 25,25")
  Espframe.position = UDim2.new("40,5 40,5")
  Espframe.visible = true
  if Espframe (
not visible then do Espframe.visible = true
end)
  
end)
  Settings.name = "Settings"
  Settings.parent = Settings
  Settings.size = UDim2.new("2,2 2,2")
  Settings.image = "rbxassetid://"
  Settings.position = UDim2.new("25,1")
  
  
```
# Need Some Chance? 
